<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Apologize</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">

  <!-- Linkvertise API -->
  <script src="https://publisher.linkvertise.com/cdn/linkvertise.js"></script>
  <script>linkvertise(572754, {whitelist: [], blacklist: [""]});</script>

  <style>
    body {
      background: linear-gradient(to right, #1e1e2f, #33038d9d);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .glass {
      background: rgba(255, 255, 255, 0.12);
      backdrop-filter: blur(15px);
      border-radius: 1.5rem;
      padding: 2rem;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.25);
    }

    .progress-bar .progress {
      transition: width 0.5s ease;
    }

    .btn-primary {
      background: linear-gradient(135deg, #ff6ec4, #7873f5);
      color: white;
      padding: 0.8rem 2rem;
      border-radius: 9999px;
      font-weight: bold;
      box-shadow: 0 0 12px rgba(255, 110, 196, 0.6);
      transition: all 0.3s ease;
    }

    .btn-primary:hover {
      transform: translateY(-3px) scale(1.05);
      box-shadow: 0 0 20px rgba(255, 110, 196, 0.8);
    }

    .btn-home {
      background: rgba(255, 255, 255, 0.2);
      border: 1px solid rgba(255, 255, 255, 0.3);
      padding: 0.6rem 1.5rem;
      border-radius: 9999px;
      font-weight: 600;
      color: #fff;
      backdrop-filter: blur(6px);
      transition: all 0.3s ease;
    }

    .btn-home:hover {
      background: rgba(255, 255, 255, 0.3);
      transform: scale(1.05);
    }

    .progress.width-1 { width: 33.33%; }
    .progress.width-2 { width: 66.66%; }
    .progress.width-3 { width: 100%; }
  </style>
</head>
<body class="text-white">

  <div class="min-h-screen flex flex-col justify-center items-center px-4">
    <header class="mb-8 text-4xl font-bold">
      <button onclick="window.location.href='/'" class="btn-home">Apologize</button>
    </header>

    <main class="glass max-w-xl w-full">
      <div class="flex flex-col items-center">
        <% if (mod.image) { %>
          <div class="w-40 h-40 mb-4 rounded-xl overflow-hidden shadow-lg border-4 border-white/30">
            <img src="<%= mod.image %>" alt="<%= mod.name %>" class="w-full h-full object-cover" />
          </div>
        <% } %>

        <h1 class="text-2xl font-bold mb-4 text-white/90"><%= mod.name %></h1>

        <div class="w-full mb-6">
          <div class="bg-white/20 h-4 rounded-full overflow-hidden">
            <div class="progress bg-pink-400 h-full <%= 'width-' + checkpoint %> rounded-full"></div>
          </div>
          <div class="flex justify-between mt-2 text-sm">
            <span class="<%= checkpoint >= 1 ? 'text-pink-200 font-bold' : 'text-white/60' %>">1</span>
            <span class="<%= checkpoint >= 2 ? 'text-pink-200 font-bold' : 'text-white/60' %>">2</span>
            <span class="<%= checkpoint >= 3 ? 'text-pink-200 font-bold' : 'text-white/60' %>">3</span>
          </div>
        </div>

        <div class="text-center mb-6">
          <h2 class="text-xl font-semibold text-white/90">Checkpoint <%= checkpoint %> of 3</h2>
          <p class="text-white/70">Please click the button "Next"</p>
        </div>

        <a href="<%= nextUrl %>" class="btn-primary text-lg">Next</a>
      </div>
    </main>

    <footer class="mt-12 text-sm text-white/60">
      &copy; <%= new Date().getFullYear() %> Apologize. All rights reserved.
    </footer>
  </div>

</body>
</html>

<!-- views/download.ejs -->
<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= mod.name %> - Download Scripts</title>
  <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #7e57c2;
      --primary-light: #b085f5;
      --primary-dark: #4d2c91;
      --secondary: #ffb74d;
      --secondary-light: #ffe9ca;
      --background: #f8f9fa;
      --card-bg: #ffffff;
      --text: #333333;
      --text-light: #666666;
      --success: #66bb6a;
      --danger: #ef5350;
      --border: #e0e0e0;
      --shadow: rgba(0, 0, 0, 0.05);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Prompt', 'Kanit', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background-color: var(--background);
      color: var(--text);
      line-height: 1.6;
    }

    .container {
      max-width: 900px;
      margin: 0 auto;
      padding: 20px;
    }

    .download-page {
      background-color: var(--card-bg);
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
      padding: 30px;
      margin-bottom: 40px;
      position: relative;
      overflow: hidden;
    }

    .download-page:before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 6px;
      background: linear-gradient(to right, var(--primary), var(--primary-light));
    }

    header {
      text-align: center;
      margin-bottom: 30px;
    }

    .page-title {
      font-size: 28px;
      font-weight: 700;
      color: var(--primary);
      margin-bottom: 10px;
    }

    .page-subtitle {
      font-size: 16px;
      color: var(--text-light);
    }

    .mod-image {
      width: 100%;
      height: 200px;
      object-fit: cover;
      border-radius: 12px;
      margin-bottom: 20px;
    }

    .scripts-container {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 20px;
      margin-top: 30px;
    }

    .script-card {
      background-color: #f8f9fa;
      border-radius: 15px;
      overflow: hidden;
      transition: all 0.3s;
      border: 1px solid var(--border);
      height: 100%;
      display: flex;
      flex-direction: column;
    }

    .script-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.08);
      border-color: var(--primary-light);
    }

    .script-img {
      width: 100%;
      height: 150px;
      object-fit: cover;
    }

    .script-details {
      padding: 15px;
      flex-grow: 1;
      display: flex;
      flex-direction: column;
    }

    .script-name {
      font-size: 18px;
      font-weight: 600;
      color: var(--primary);
      margin-bottom: 10px;
    }

    .script-description {
      color: var(--text-light);
      font-size: 14px;
      margin-bottom: 15px;
      flex-grow: 1;
    }

    .btn {
      display: inline-block;
      padding: 12px 20px;
      background-color: var(--primary);
      color: white;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      font-weight: 500;
      text-decoration: none;
      transition: all 0.3s ease;
      font-size: 16px;
      text-align: center;
    }

    .btn-download {
      background: linear-gradient(135deg, var(--primary), var(--primary-dark));
      width: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
    }

    .btn-download:hover {
      background: linear-gradient(135deg, var(--primary-dark), var(--primary-dark));
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    }

    .btn-download:before {
      content: '⬇️';
      font-size: 18px;
    }

    footer {
      text-align: center;
      padding: 20px 0;
      color: var(--text-light);
      font-size: 14px;
    }

    @media (max-width: 768px) {
      .scripts-container {
        grid-template-columns: 1fr 1fr;
      }
    }

    @media (max-width: 576px) {
      .scripts-container {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <main class="download-page">
      <header>
        <h1 class="page-title"><%= mod.name %></h1>
        <p class="page-subtitle">เลือกสคริปต์ที่คุณต้องการดาวน์โหลด</p>
      </header>

      <% if (mod.image) { %>
        <img src="<%= mod.image %>" alt="<%= mod.name %>" class="mod-image">
      <% } %>

      <div class="scripts-container">
        <% mod.scripts.forEach((script, index) => { %>
          <div class="script-card">
            <% if (script.image) { %>
              <img src="<%= script.image %>" alt="<%= script.name %>" class="script-img">
            <% } else { %>
              <div class="script-img" style="background-color: #e9ecef; display: flex; align-items: center; justify-content: center;">
                <span style="font-size: 40px; color: #adb5bd;">📜</span>
              </div>
            <% } %>
            <div class="script-details">
              <h3 class="script-name"><%= script.name %></h3>
              <% if (script.description) { %>
                <p class="script-description"><%= script.description %></p>
              <% } else { %>
                <p class="script-description">คลิกเพื่อดาวน์โหลดสคริปต์นี้</p>
              <% } %>
              <a href="/mod/<%= mod.shortId %>/download/<%= index %>" class="btn btn-download">ดาวน์โหลด</a>
            </div>
          </div>
        <% }) %>
      </div>
    </main>

    <footer>
      <p>&copy; <%= new Date().getFullYear() %> Free Mod. All rights reserved. Made with ❤️</p>
    </footer>
  </div>
</body>
</html>